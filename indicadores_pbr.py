# indicadores_pbr.py
import pandas as pd
import dash_bootstrap_components as dbc
from dash import html, dash_table

def calcular_indicadores_pbr(df_tabla_activa=None, nombre_tabla_activa=""):
    """
    Sección desactivada por completo.
    Retorna un contenedor vacío sin procesar archivos ni mostrar elementos.
    """
    return html.Div()
